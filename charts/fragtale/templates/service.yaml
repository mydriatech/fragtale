apiVersion: v1
kind: Service
metadata:
  name: {{ include "fragtale.fullname" . }}
  labels:
    {{- include "fragtale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fragtale.selectorLabels" . | nindent 4 }}
