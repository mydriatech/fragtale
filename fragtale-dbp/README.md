# Fragtale database provider interface

The crate provides an asynchronous database provider interface and common
objects for interacting the with interface.

The interface is further subdivided into facades for each general area of
application logic.
